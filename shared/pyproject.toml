[project]
name = "family-car-agent-shared"
version = "0.1.0"
description = "Shared models for Family Car Agent"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
